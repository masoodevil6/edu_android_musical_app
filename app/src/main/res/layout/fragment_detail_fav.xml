<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layoutDirection="rtl"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/shape_background">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:background="@drawable/shape_toolbar"
        android:layout_margin="10dp"
        android:elevation="20dp">

        <TextView
            android:id="@id/element_id_fragment_favorite_detail_txt_title_category_fav"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="مورد علاقه ها"
            android:textColor="@color/item_color"
            android:textSize="18sp" />

        <ImageView
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:src="@drawable/baseline_arrow_back_24"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="8dp"
            android:id="@id/element_id_fragment_favorite_image_back"
            android:rotation="180"
            />

    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@id/element_id_fragment_favorite_detail_txt_recycler_view_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginStart="10dp"
        />

</LinearLayout>